#include <stdio.h>

/**
 * main - Run the code Kronk!
 * @argc: The arg count
 * @argv: all the args
 *
 * Return: 0
 */
int main(int argc, int *argv[])
{
	if (argc > 0)
		print("%s\n", argv[0])

	return (0);
}
