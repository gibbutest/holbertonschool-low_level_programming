#include <stdio.h>

/**
 * main - Run the code Kronk!
 * @argc: The arg count
 * @argv: all the args
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	if (argc > 0)
		printf("%s\n", argv[0]);

	return (0);
}
